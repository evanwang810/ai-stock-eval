# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg
*.egg-info/
dist/
build/
*.whl

# Virtual environment
.venv/
venv/
env/
.env/

# Environment / secrets  — keep .env.example tracked, ignore everything real
.env
.env.*
!.env.example
*.env
secrets.py
keys.py
keys.txt
watcher/config.json

# Watcher runtime output
watcher/results/

# Headlines split off the public data archive (Finnhub ToS).
# Uploaded by the workflow as a private artifact instead of being committed.
data/private/

# Test / coverage caches
.pytest_cache/
.coverage
htmlcov/
.tox/

# IDE
.idea/
.vscode/
*.iml

# Logs produced at runtime  (keep the logs/ folder via .gitkeep)
logs/*.jsonl

# OS
.DS_Store
Thumbs.db
